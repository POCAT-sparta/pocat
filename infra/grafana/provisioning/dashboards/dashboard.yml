apiVersion: 1

providers:
  - name: 'POCAT Local'
    orgId: 1
    folder: 'POCAT'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
